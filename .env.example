# LifeOS Environment Variables
# Copy this to .env and fill in your values
# NEVER commit .env to version control

# ═══════════════════════════════════════════════════════════════
# PROVIDERS
# ═══════════════════════════════════════════════════════════════

# ─── GitHub ────────────────────────────────────────────────────
# Vault access via GitHub API
# Fine-grained PAT scoped to your vault repo (Contents: read/write)
GITHUB_PAT=ghp_your_token_here
GITHUB_REPO_OWNER=your-github-username
GITHUB_REPO_NAME=your-vault-repo-name
GITHUB_BRANCH=main

# ─── Google ────────────────────────────────────────────────────
# OAuth credentials (console.cloud.google.com → APIs & Services → Credentials)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Account tokens — obtained by running: npm run auth
# Add one per Google account. Key uses the account alias.
GOOGLE_TOKEN_PERSONAL='{"refresh_token":"your-refresh-token","access_token":"","expiry_date":null}'
# GOOGLE_TOKEN_WORK='{"refresh_token":"...","access_token":"...","expiry_date":null}'

# Account routing — JSON array of account configs (see docs/setup-guide.md)
ACCOUNTS_CONFIG='[{"alias":"personal","email":"you@gmail.com","type":"personal","projects":["*"],"isDefaultDraft":true,"isDefaultTasks":true}]'

# Google AI / Gemini — primary AI for Telegram bot (free tier: 1,500 req/day)
# Get a free API key at: https://aistudio.google.com/apikey
# Falls back to Claude automatically on errors or quota exhaustion
GOOGLE_AI_API_KEY=your-gemini-api-key
# GEMINI_MODEL=gemini-2.5-flash

# ─── Anthropic ─────────────────────────────────────────────────
# Claude API — fallback for Telegram bot + background agents
ANTHROPIC_API_KEY=sk-ant-your-key-here
# Model for Claude fallback (default: claude-opus-4-6)
# Options: claude-opus-4-6, claude-sonnet-4-20250514, claude-haiku-3-5-20241022
# CLAUDE_MODEL=claude-opus-4-6

# ─── Zapier ────────────────────────────────────────────────────
# Secret to verify incoming webhooks from Zapier (for Granola)
ZAPIER_WEBHOOK_SECRET=your-webhook-secret

# ═══════════════════════════════════════════════════════════════
# INFRASTRUCTURE
# ═══════════════════════════════════════════════════════════════

# ─── GCP ───────────────────────────────────────────────────────
GCP_PROJECT_ID=your-gcp-project
GCP_REGION=us-central1

# ─── Telegram Bot ──────────────────────────────────────────────
# Create a bot via @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your-bot-token-from-botfather
# Your Telegram user ID (get from @userinfobot)
TELEGRAM_CHAT_ID=your-telegram-user-id
# Comma-separated user IDs allowed to use the bot
TELEGRAM_ALLOWED_USERS=your-telegram-user-id
# Webhook secret (generate with: openssl rand -hex 32)
TELEGRAM_WEBHOOK_SECRET=your-webhook-secret
# Cloud Run URL for the Telegram channel service
# TELEGRAM_WEBHOOK_URL=https://lifeos-channel-telegram-xxx.run.app/webhook
# Agent URLs for Telegram commands
# AGENT_BRIEFING_URL=https://lifeos-agent-briefing-xxx.run.app
# AGENT_RESEARCH_URL=https://lifeos-agent-research-xxx.run.app

# ═══════════════════════════════════════════════════════════════
# OPTIONS
# ═══════════════════════════════════════════════════════════════

# ─── Vault Structure ──────────────────────────────────────────
# Customize via AI setup or manually. Defaults work out of the box.
# VAULT_CATEGORIES='["Consulting","Ideas","Open Source","Archive"]'
# PROJECT_SUBFOLDERS='["files"]'
# PROJECT_TAGS='["status/active","status/paused","status/done","type/client","type/product","type/personal"]'

# ─── General ──────────────────────────────────────────────────
# Your timezone (IANA format, e.g., "America/New_York", "Europe/London")
TIMEZONE=UTC
# Log level: debug, info, warn, error
LOG_LEVEL=info
# Ports: each server has its own default (3001–3008), no override needed
