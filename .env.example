# LifeOS Environment Variables
# Copy this to .env and fill in your values
# NEVER commit .env to version control

# ─── GitHub (vault access) ──────────────────────────────────
# Fine-grained PAT scoped to your vault repo (Contents: read/write)
GITHUB_PAT=ghp_your_token_here
GITHUB_REPO_OWNER=your-github-username
GITHUB_REPO_NAME=your-vault-repo-name
GITHUB_BRANCH=main

# ─── Google OAuth ────────────────────────────────────────────
# Create at: console.cloud.google.com → APIs & Services → Credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# ─── Google Account Tokens ───────────────────────────────────
# Obtained by running: npm run auth
# The auth script outputs the correct env key and JSON value.
# Add one token per Google account. The key uses the account alias
# (e.g., GOOGLE_TOKEN_PERSONAL for --alias=personal).
GOOGLE_TOKEN_PERSONAL='{"refresh_token":"your-refresh-token","access_token":"","expiry_date":null}'
# GOOGLE_TOKEN_WORK='{"refresh_token":"...","access_token":"...","expiry_date":null}'

# ─── Account Configuration ───────────────────────────────────
# JSON array of account configs (see docs/setup-guide.md)
# Each account needs: alias, email, type, projects[], flags
ACCOUNTS_CONFIG='[{"alias":"personal","email":"you@gmail.com","type":"personal","projects":["*"],"isDefaultDraft":true,"isDefaultTasks":true}]'

# ─── Anthropic (for background agents) ──────────────────────
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ─── Zapier (for Granola webhook) ────────────────────────────
# Secret to verify incoming webhooks from Zapier
ZAPIER_WEBHOOK_SECRET=your-webhook-secret

# ─── GCP (for deployment) ────────────────────────────────────
GCP_PROJECT_ID=your-gcp-project
GCP_REGION=us-central1

# ─── Vault Structure ─────────────────────────────────────────
# Customize via AI setup or manually. Defaults work out of the box.
# VAULT_CATEGORIES='["Consulting","SaaS","Business","Archive"]'
# PROJECT_SUBFOLDERS='["files"]'
# PROJECT_TAGS='["status/active","status/paused","status/done","type/client","type/product","type/personal"]'
# INBOX_STYLE=by-contact

# ─── Optional ────────────────────────────────────────────────
# Your timezone (IANA format, e.g., "America/New_York", "Europe/London")
TIMEZONE=UTC
# Log level: debug, info, warn, error
LOG_LEVEL=info
# Ports: each server has its own default (3001–3007), no override needed
