{
  "name": "@lifeos/shared",
  "version": "0.1.0",
  "description": "Shared utilities for LifeOS: Google auth, vault access, contacts, config",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b --force",
    "dev": "tsc -b --watch"
  },
  "dependencies": {
    "@octokit/rest": "^21.0.0",
    "googleapis": "^144.0.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}
